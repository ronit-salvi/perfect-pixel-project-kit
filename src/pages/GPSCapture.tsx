
import React from "react";
import GPS from "@/components/gps/GPS";

const GPSCapture: React.FC = () => {
  return (
    <main className="min-h-screen bg-gray-50">
      <GPS />
    </main>
  );
};

export default GPSCapture;
